<h1><%= @category.name %>  total amount: $<%= @total %></h1>

<% @purchases.each do |purchase| %>
<p>
	<%= purchase.entity.name %>
	$ <%= purchase.entity.amount %>
</p>
	<br>
<% end %>

<%= link_to 'Back', authenticated_root_path%>
